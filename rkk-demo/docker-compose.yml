services:
  rkk-demo:
    build:
      context: ..
      dockerfile: rkk-demo/Dockerfile
    ports:
      - "8080:80"
    environment:
      VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:3000/api/v1}
